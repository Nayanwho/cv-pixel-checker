version: '3.8'

services:
  cv-pixel-checker:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
      - API_KEY=dev-secret-key
      - REQUIRE_AUTH=false
    restart: unless-stopped
