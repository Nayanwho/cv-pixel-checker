# CV Pixel Checker Measurement Service Environment Variables

# Server Port (default: 3000)
PORT=3000

# Node Environment
NODE_ENV=production

# Bearer API Key for authentication (Optional in dev, required in production)
API_KEY=dev-secret-key

# Enforce strict API authentication (true/false)
REQUIRE_AUTH=false
